# Copy to `.env` and fill in values. Never commit `.env`.

# Coinbase (Advanced Trade) API credentials (trade-only key; withdrawals disabled)
COINBASE_API_KEY=
COINBASE_API_SECRET=

# Optional: set to 1 to enable extra debug logging
DEBUG=

